services:
  - type: web
    name: bro
    runtime: docker
    plan: starter
    region: frankfurt
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: RESOLUTION
        value: 1280x800
      - key: VNC_PASSWORD
        sync: false
